{
  "mcpServers": {
    "git": {
      "args": [
        "mcp-server-git",
        "--repository",
        "${CWD}"
      ],
      "command": "uvx",
      "disabled": false,
      "env": {
        "HTTPS_PROXY": "${HTTPS_PROXY}",
        "HTTP_PROXY": "${HTTP_PROXY}"
      }
    }
  }
}
